# Ninj-OS Vanilla Host Agent

The host agent complements the Vanilla Bridge behavior pack. It reports BDS process health and synchronizes verified network operators into the server's `permissions.json`. It builds for Linux and Windows from one Go source tree.

The behavior pack controls the live player session. The host agent controls host/process information. A full-proxy vanilla backend normally uses both.

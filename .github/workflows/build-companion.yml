name: Build Endstone Companion

on:
  workflow_dispatch:
    inputs:
      endstone_version:
        description: Exact Endstone release without the leading v
        required: true
        default: '0.11.6'
        type: string
  push:
    tags: ['v*']
  pull_request:
    paths:
      - 'companion/**'
      - '.github/workflows/build-companion.yml'

permissions:
  contents: write

env:
  DEFAULT_ENDSTONE_VERSION: '0.11.6'

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    container: ubuntu:22.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Select Endstone version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          version="${{ inputs.endstone_version || '0.11.6' }}"
          version="${version#v}"
          [[ "$version" =~ ^[0-9]+\.[0-9]+(\.[0-9]+)?$ ]] || { echo "Invalid Endstone version: $version" >&2; exit 1; }
          echo "endstone_version=$version" >> "$GITHUB_OUTPUT"

      - name: Install build tools
        shell: bash
        run: |
          set -euo pipefail
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y ca-certificates cmake curl file gh git gnupg ninja-build python3 python3-dev python3-pip unzip zip
          curl -fsSL https://apt.llvm.org/llvm-snapshot.gpg.key \
            | gpg --dearmor -o /usr/share/keyrings/llvm-snapshot.gpg
          echo "deb [signed-by=/usr/share/keyrings/llvm-snapshot.gpg] http://apt.llvm.org/jammy/ llvm-toolchain-jammy-18 main" \
            > /etc/apt/sources.list.d/llvm-18.list
          apt-get update
          apt-get install -y clang-18 libc++-18-dev libc++abi-18-dev lld-18

      - name: Generate companion documentation
        run: python3 companion/scripts/generate-documentation.py

      - name: Configure and compile
        env:
          ENDSTONE_VERSION: ${{ steps.version.outputs.endstone_version }}
        run: |
          cmake -S companion -B companion/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DENDSTONE_API_VERSION="$ENDSTONE_VERSION" \
            -DCMAKE_C_COMPILER=clang-18 \
            -DCMAKE_CXX_COMPILER=clang++-18 \
            -DCMAKE_CXX_FLAGS='-stdlib=libc++' \
            -DCMAKE_SHARED_LINKER_FLAGS='-stdlib=libc++'
          cmake --build companion/build --parallel 2

      - name: Build Ninj-OS Endstone identity runtime
        env:
          ENDSTONE_VERSION: ${{ steps.version.outputs.endstone_version }}
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 --branch "v${ENDSTONE_VERSION}" https://github.com/EndstoneMC/endstone.git companion/endstone-runtime/source
          git -C companion/endstone-runtime/source apply ../ninjos-native-identity.patch
          python3 -m pip install --upgrade build conan conan-py-build
          (
            cd companion/endstone-runtime/source
            CC=clang-18 CXX=clang++-18 python3 -m build --wheel
          )
          wheel="$(find companion/endstone-runtime/source/dist -type f -name '*.whl' -print -quit)"
          [[ -f "$wheel" ]] || { echo 'Patched Endstone wheel was not produced.' >&2; exit 1; }
          mkdir -p companion/build/ninjos-endstone-runtime
          cp "$wheel" companion/build/ninjos-endstone-runtime/

      - name: Package companion
        id: package
        shell: bash
        env:
          ENDSTONE_VERSION: ${{ steps.version.outputs.endstone_version }}
        run: |
          set -euo pipefail
          plugin="$(find companion/build -type f -name 'ninjos_proxie_companion.so' -print -quit)"
          [[ -f "$plugin" ]] || { echo 'Companion binary was not produced.' >&2; exit 1; }
          root="dist/companion-package"
          mkdir -p "$root/plugins/ninjos_proxie_companion"
          cp "$plugin" "$root/plugins/ninjos_proxie_companion.so"
          cp companion/companion.properties.example "$root/plugins/ninjos_proxie_companion/companion.properties"
          cp companion/docs/COMPLETE_SETUP.md "$root/COMPANION-HOWTO.md"
          cp companion/docs/CONFIGURATION.md "$root/CONFIGURATION.md"
          mkdir -p "$root/ninjos-endstone-runtime"
          cp companion/build/ninjos-endstone-runtime/*.whl "$root/ninjos-endstone-runtime/"
          cp companion/endstone-runtime/INSTALL.md "$root/ninjos-endstone-runtime/INSTALL.md"
          cat > "$root/BUILD-INFO.txt" <<INFO
          Plugin: Ninj-OS Endstone Companion
          Plugin version: 3.7.0
          Endstone version: ${ENDSTONE_VERSION}
          Platform: Linux x86_64
          Git commit: ${GITHUB_SHA}
          INFO
          (cd "$root" && sha256sum plugins/ninjos_proxie_companion.so ninjos-endstone-runtime/*.whl > SHA256SUMS.txt)
          archive="NinjOS-Endstone-Companion-v3.7.0-Endstone-${ENDSTONE_VERSION}-Linux-x86_64.zip"
          (cd "$root" && zip -r "../$archive" .)
          sha256sum "dist/$archive" > "dist/$archive.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: Endstone-Companion-${{ steps.version.outputs.endstone_version }}
          path: |
            dist/*.zip
            dist/*.sha256

      - name: Attach to tagged release
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          for attempt in {1..60}; do
            if gh release view "${GITHUB_REF_NAME}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
              gh release upload "${GITHUB_REF_NAME}" dist/*.zip --clobber --repo "${GITHUB_REPOSITORY}"
              exit 0
            fi
            sleep 10
          done
          echo "Release ${GITHUB_REF_NAME} was not created within 10 minutes." >&2
          exit 1

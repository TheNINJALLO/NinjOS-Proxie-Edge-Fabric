# Windows deployment files

Use the root `install-windows.ps1` script. It installs the Linux runtime inside WSL2, configures mirrored networking, adds Windows and Hyper-V firewall rules, and creates the startup task.

Management commands are provided by `manage-windows.ps1`.
